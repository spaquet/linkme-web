---
// Define the props
export interface Props {
  isMobile?: boolean;
}

// Define navigation items
const navItems = [
  { text: "Features", href: "/features" },
  { text: "Pricing", href: "/pricing" },
  { text: "About", href: "/about" },
  { text: "Support", href: "/support" },
];

const { isMobile = false } = Astro.props;
---

<nav class={isMobile ? "flex flex-col space-y-2" : "flex items-center space-x-8"}>
  {
    navItems.map((item) => (
      <a
        href={item.href}
        class={`
          font-medium transition-colors duration-200
          ${isMobile 
            ? "block py-2 text-white hover:text-indigo-300" 
            : "text-sm text-white hover:text-indigo-300"
          }
        `}
      >
        {item.text}
      </a>
    ))
  }
</nav>